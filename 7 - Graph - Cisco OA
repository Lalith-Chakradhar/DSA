/* package whatever; // don't place package name! */

import java.util.*;
import java.lang.*;
import java.io.*;

/* Name of the class has to be "Main" only if the class is public. */
class Ideone
{
	static void dfs(Set<Integer> adjList, int[] used, int source, int[] compNum, int c)
	{
		used[source] = 1;
		compNum[source] = c;
		for(int child: adjList[source])
		{
			if(used[child]==0)
			{
				used[]
			}
		}
	}
	public static void main (String[] args) throws java.lang.Exception
	{
		Scanner sc = new Scanner(System.in);
		int pods = sc.nextInt();
		int n = sc.nextInt();
		
		Set<Integer>[] adjList = new TreeSet[pods+1];
		
		for(int i=1; i<pods+1; i++)
		{
			adjList[i] = new TreeSet<Integer>();
		}
		
		for(int i=0; i<n; i++)
		{
			int x = sc.nextInt();
			int y = sc.nextInt();
			
			adjList[x].add(y);
			adjList[y].add(x);
		}
		
		int[] used = new int[pods+1];
		int[] compNum = new int[pods+1];
		int c = 0;
		
		for(int i=1; i<pods+1; i++)
		{
			if(used[i] == 0)
			{
				c++;
				dfs(adjList, used, i,compNum, c);
			}
		}
		
		int q = sc.nextInt();
		int[][] queries = new int[q][2];
		int[] mark = new int[n+1];
		
		for(int i=0; i<q; i++)
		{
			queries[i][0] = sc.nextInt();
			queries[i][1] = sc.nextInt();
			
			if(queries[i][0] == 2)
			{
				mark[queries[i][1]] = 1;
				//find in p and then remove it
			}
			else if(queries[i][0] == 1)
			{
				if(mark[queries[i][1]] == 0)
				{
					System.out.print(queries[i][1]+" ");
				}
				else
				{
					//find first in p and then get the top element	
				}
			}
		}
	}
}
