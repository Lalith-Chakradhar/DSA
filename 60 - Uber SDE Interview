class Main {

    // Algorithm
    // 1. Find the possible cases of the absolute values:
    //    (a[i] + i) - (a[j] + a[k] + j + k),
    //    (a[j] + a[k] + j + k) - (a[i] + i),
    //    (a[i] - i) + (j-a[j] + k-a[k]),
    //    (i - a[i]) + (a[j]-j + a[k]-k)
    // 2. Find 1 max, 2 min for case 1, 2 max , 1 min for case 2 , 3 max for case 3 and 4 . Take the max result of all 4.
    static int findMaxValue(int[] arr) {
        
    }

    public static void main(String args[]) {
        Scanner sc = new Scanner(System.in);

        int n = sc.nextInt();
        int[] arr = new int[n];

        for (int i = 0; i < n; i++) {
            arr[i] = sc.nextInt();
        }

        System.out.println(
            "Maximum value |a[i] - a[j] - a[k]| + |i-j-k|: "
            + findMaxValue(arr)
        );
    }
}
